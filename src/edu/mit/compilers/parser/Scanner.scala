/**
 * THIS IS JUST A SUGGESTED CLASS, YOU CAN MODIFY OR ADD ANYTHING YOU WISH
 */

package edu.mit.compilers.parser

class Scanner(string: String) {

  /** Reveals the next `numTokens` from the text `string` */
  def peek(numTokens: Int): Seq[Token] = {
    Seq.empty
  }

  /** Scans the entire text `string` and returns a collection of all the tokens, in order */
  def scan(): Seq[Token] = {
    Seq.empty
  }

  /** Whether to print debugging info or not*/
  def setTrace(debug: Boolean) {}
}
